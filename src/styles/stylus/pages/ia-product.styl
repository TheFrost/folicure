/*Local Mixin*/
set-color-theme(gradient, opacity-value, nav, header-thumbs, icon-modifier)
  if !opacity-value
    opacity-value = 1

  .ia-Product
    &__inner
      background-linear-gradient(180deg, rgba(white,0.1), 0, rgba(gradient,opacity-value), 130%)
      +above(991px)
        background-linear-gradient(180deg, rgba(white,0.1), 45%, rgba(gradient,opacity-value), 100%)
    &__header
      &:after
        background-color header-thumbs
    &__category
      background-color header-thumbs
    &__type
      color header-thumbs
    &__nav
      &__next,
      &__prev,
      &__info
        color nav
  .ia-Thumbs
    &__item
      &.js-active-thumb
        background-color header-thumbs
    &__trigger
      &:after
        content url('../images/icon-bottle--'icon-modifier'.svg')


/*Product Card*/
.ia-Product
  position relative
  text-align center
  background-color c__gray--teta
  background-repeat no-repeat
  background-position center center
  background-size 100%
  background-attachment fixed
  +above(991px)
    background-size auto
    overflow hidden
    height 100%
  &__return
    fixed 59px 0 false false
    z-index 100
    display block
    padding 8px 16px
    text-transform uppercase
    background-color c__gray
    color white
    transition all 0.33s easing( 'in-out-cubic' )
    +above(991px)
      right 32px
      top 100px
    +above(1025px)
      right 100px
    &:hover
      background-color c__gray--delta
    &:before
      content '\f104'
      font normal normal normal 14px/1 FontAwesome
      font-size 16px
      margin-right 8px
  &__inner
    clearfix()
    background-attachment fixed
    +above(991px)
      height 100%
  &__carousel
    position relative
    +above(991px)
      absolute 50% false false 0
      width 100%
      overflow: hidden;
      transform translateY(-50%)
  &__column
    +above(991px)
      span(1/2)
  &__item
    clearfix()
    display inline-block
    padding 32px
    width 100%
    max-width 900px
    +above(991px)
      padding 96px
    +above(1400px)
      padding 32px
  &__fig
    margin-bottom 96px
    +above(991px)
      margin-bottom 32px
    img
      max-width 100%
    &--d
      display none
      +above(991px)
        display inline-block
    &--m
      display inline-block
      +above(991px)
        display none
  &__desc
    padding 16px
    +above(991px)
      padding-top 108px
  &__header
    &:after
      content ''
      display inline-block
      width 50px
      height 8px
      margin 32px 0
  &__category,
  &__type
    margin 0
    padding 6px 16px 2px
    font-size 28px
    line-height 1
    letter-spacing 6px
    text-transform uppercase
    +above(991px)
      font-size 48px
  &__category
    margin-bottom 4px
    font-family NexaLight
    color white
    font-size 20px
    +above(991px)
      font-size 32px
  &__type
    font-family NexaBold
    font-size 12px
    +above(991px)
      font-size 20px
  &__text
    margin-top 0
    margin-bottom 32px
    font-size 18px
    color c__black
    +above(991px)
      text-align left
    &--note
      color c__gray
  &__share
    /*display inline-block*/
    display none
    width 50px
    margin-top 64px
    border 1px solid c__black
    transform rotate(45deg)
    transition all 0.33s easing( 'in-out-cubic' )
    &:hover
      transform rotate(90deg)
      img
        transform rotate(-90deg)
    img
      transition all 0.33s easing( 'in-out-cubic' )
      transform rotate(-45deg)
  &__nav
    fixed false 0 43px false
    z-index 100
    width 100%
    +above(991px)
      fixed false 0 50% false
      width auto
      transform translateY(50%)
    &__next,
    &__prev,
    &__info
      display block
      height 40px
      line-height 40px
      text-align center
      background-color c__gray
      +above(991px)
        float none !important
        width 40px !important
    &__info
      span(2/4)
    &__count,
    &__total
      font-size 14px
      color white
    &__next,
    &__prev
      span(1/4)
      &:hover
        background-color c__gray--delta

      &.js-disabled
        background-color lighten(c__gray, 20%)
    &__prev
      border-right 1px solid c__gray--gama
      +above(991px)
        border-right none
        border-bottom 1px solid c__gray--gama
    &__next
      border-left 1px solid c__gray--gama
      +above(991px)
        border-left none
        border-top 1px solid c__gray--gama



/*Thumbs Control*/
.ia-Thumbs
  fixed false false 43px false
  z-index 100
  display none
  width 100%
  max-height 200px
  transform translateY(100%)
  transition all 0.75s easing( 'in-out-cubic' )
  +above(991px)
    display block
  +above(991px)
    width calc(100% \- 150px)

  &.js-thumbs-active
    transform translateY(0)
  &__header
    display inline-block
    absolute false false false 50%
    margin-top -43px
    transform translateX(-50%)
  &__trigger
    position relative
    display block
    padding 12px 48px 12px 32px
    font-size 14px
    font-weight 300
    text-transform uppercase
    color white
    background-color c__gray
    &:hover
      background-color c__gray--delta
    &:after
      display block
      absolute 50% 23px false false
      z-index 100
      size 13px 33px
      transform translateY(-50%)
  &__body
    clearfix()
    background-color c__gray
    border 1px solid c__gray--delta
  &__item
    position relative
    float left
    min-height 200px
    padding 20px
    text-align center
    cursor pointer
    border-left 1px solid c__gray--delta
    &:hover
      background-color c__gray--delta
    &:nth-child(1)
      border none
  &__media
    display inline-block
    max-height 120px
    max-width 100%
  &__caption
    absolute false 16px 16px
    font-family NexaBold
    font-size 10px
    text-transform uppercase
    letter-spacing 1px
    color white
    +above(1199px)
      font-size 14px



/*Colors control*/
.ia-Product
  &--extra
    set-color-theme(c__extra--alpha, 0.7, c__extra--alpha, c__extra, extra)
  &--hidratante
    set-color-theme(c__hidratante, 0.3, c__hidratante--alpha, c__hidratante, hidratante)
  &--original
    set-color-theme(c__original, 0.3, c__original--beta, c__original, original)
  &--control
    set-color-theme(c__control, 0.3, false, c__control, false)


/*Fix chrome images problem with Owl carousel*/
/* fix */
.owl-carousel  .owl-wrapper,
.owl-carousel  .owl-item
  backface-visibility hidden
  transform translate3d(0,0,0)
